return {
  "folke/snacks.nvim",
  opts = {
    picker = {
      -- hidden = true,   -- Shows hidden files (dotfiles)
      -- ignored = true,  -- Shows files ignored by .gitignore
      sources = {
          explorer = {
            layout = {
              -- auto_hide = { "input" }, 
            },
          },
    },
    },
  },
}
